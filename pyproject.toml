[project]
name = "yfin-client"
version = "1.0.0"
description = "Compact Python 3.14 Yahoo Finance client returning pyarrow.Table, built on fastreq."
readme = "README.md"
license = "MIT"
requires-python = ">=3.14"
authors = [{ name = "Volker Lorrmann", email = "volker.lorrmann@gmail.com" }]
dependencies = [
    "fastreq>=3.0.0",
    "pyarrow>=18.0.0",
]

[project.optional-dependencies]
polars = ["polars>=1.0.0"]

[dependency-groups]
dev = [
    "ipython>=9.15.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
    "ruff>=0.8.0",
    "stealthkit>=1.0.3",
    "ty>=0.0.1a1",
    "yahfin>=1.1.1",
    "yahooquery>=2.4.1",
]

[build-system]
requires = ["uv_build>=0.5.0,<0.12"]
build-backend = "uv_build"

[tool.uv]
# Requires fastreq>=3.0.0, published to PyPI as fastreq 3.0.0.

[tool.uv.build-backend]
module-name = "yfin"
module-root = "src"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
filterwarnings = [
    "error",
    "ignore::DeprecationWarning",
]

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["PLC0415"]  # local imports in test methods are fine

[tool.ruff.lint.pylint]
max-args = 10

[tool.ty.environment]
python-version = "3.14"
